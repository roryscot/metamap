name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npm run format:check
      - run: npm run typecheck
      - run: npm test
      - run: npm run example:generate
      - run: npm run example:check
      - run: npm run example:compile
      - run: npm run example:research
      - run: npm run example:routing
      - run: npm run example:path-tree
      - name: Ensure example outputs are current
        run: git diff --exit-code examples/workspace/generated examples/research/generated examples/routing/generated examples/path-tree/generated examples/example-generation.json
      - run: npm pack --dry-run
